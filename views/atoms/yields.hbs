{{#any yields}}
    <dt>{{translateHeading 'yields' yields}}</dt>
    {{#each yields}}
        <dd>
            {{~#markdown~}}
                {{~#all type type.parsedType~}}
                    <code>{{describeType type.parsedType}}</code>&nbsp;
                {{~/all~}}
                {{~resolveLinks description~}}
            {{~/markdown~}}
        </dd>
    {{/each}}
{{/any}}
