{{#if package}}
    <header {{~cssClass '!page-header'}}>
        <h {{~cssClass 'page-title'}}>
            {{package.name}}{{#if package.version}} {{package.version}}{{/if}}
        </h>
    </header>
{{/if}}
